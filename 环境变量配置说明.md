# ⚠️ 环境变量配置更正说明

## 📝 正确的 `.env.local` 文件格式

您的 `.env.local` 文件应该包含以下内容：

```env
# Supabase 配置（必需）
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# 豆包 AI 配置（可选）
NEXT_PUBLIC_DOUBAO_API_KEY=your_doubao_api_key
```

---

## ❌ 之前的错误配置

之前我创建的配置**有误**，包含了这三个错误的变量：
- ~~DOUBAO_APP_ID~~
- ~~DOUBAO_TOKEN~~
- ~~DOUBAO_CLUSTER~~

**请删除这三行！** 代码中实际只使用 `NEXT_PUBLIC_DOUBAO_API_KEY` 这一个变量。

---

## ✅ 正确的配置步骤

### 1. Supabase 配置（必需）

#### 获取 Supabase URL 和 Key：
1. 访问 https://supabase.com 并登录
2. 选择您的项目
3. 点击左侧 **Settings** → **API**
4. 复制以下信息：
   - **Project URL** → 粘贴到 `NEXT_PUBLIC_SUPABASE_URL`
   - **anon public** key → 粘贴到 `NEXT_PUBLIC_SUPABASE_ANON_KEY`

**示例：**
```env
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

---

### 2. 豆包 AI 配置（可选，用于 AI 助手）

#### 获取豆包 API Key：

**步骤 1：访问火山引擎控制台**
- 直接访问：https://console.volcengine.com/ark/region:ark+cn-beijing/apiKey

**步骤 2：注册/登录**
- 如果没有账号，用手机号注册
- 登录后会进入火山方舟（ARK）控制台

**步骤 3：创建 API Key**
1. 在左侧菜单找到 **API Key 管理**
2. 点击右上角 **创建 API Key** 按钮
3. 填写密钥名称（比如：任务管理器）
4. 点击确认

**步骤 4：复制 API Key**
- 系统会生成一个 API Key
- ⚠️ **注意**：密钥只显示一次，请立即复制保存！
- 点击复制按钮，粘贴到 `.env.local` 的 `NEXT_PUBLIC_DOUBAO_API_KEY`

**示例：**
```env
NEXT_PUBLIC_DOUBAO_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

---

## 💰 费用说明

**Supabase：**
- 免费套餐包含：500MB 数据库、1GB 文件存储、50,000 次月活跃用户
- 个人使用完全够用

**豆包 AI：**
- 新用户通常有免费额度
- 按 API 调用次数计费
- 如果不需要 AI 助手功能，可以不配置

---

## 🎯 配置完成后

修改好 `.env.local` 文件后：

1. **保存文件**
2. **重启开发服务器**：
   ```bash
   # 按 Ctrl+C 停止当前服务器
   # 然后重新运行
   npm run dev
   ```
3. **访问** http://localhost:3000
4. **测试功能**：
   - 注册/登录 → 测试 Supabase 连接
   - 点击 AI 助手图标 → 测试豆包 AI（如果配置了）

---

## 🆘 遇到问题？

**问题 1：提示 API Key 未配置**
- 检查 `.env.local` 文件中的变量名是否正确（包括 `NEXT_PUBLIC_` 前缀）
- 确认已重启开发服务器

**问题 2：Supabase 连接失败**
- 检查 URL 和 Key 是否完整复制（注意不要有多余空格）
- 确认 Supabase 项目状态正常

**问题 3：豆包 AI 不工作**
- 检查 API Key 是否有效
- 确认火山引擎账户余额充足
- 查看浏览器控制台（F12）的错误信息





