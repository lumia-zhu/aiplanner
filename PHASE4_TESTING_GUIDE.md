# 🧪 Phase 4 UI 集成测试指南

## 📊 测试概览

本文档包含 **8 个测试场景**，用于验证 Agent UI 集成的完整功能。

**测试环境**：
- 确保已登录
- 确保 API Key 已配置
- 打开浏览器控制台（F12）查看日志
- 访问 `http://localhost:3000/dashboard`

---

## 🎯 测试场景清单

### ✅ 场景 1: 简单对话（不调用工具）

**目的**：验证 Agent 能正确处理简单对话，不调用任何工具。

**步骤**：
1. 打开 Dashboard，点击右下角 AI 助手图标
2. 切换到 **Agent 模式**（顶部开关）
3. 输入：`你好`
4. 发送消息

**预期结果**：
- ✅ 显示 1 个 **Agent Thought** 卡片（紫色）
  - 内容类似："这是一个简单的问候，不需要调用工具..."
  - 显示迭代次数：1
  - 显示时间戳
- ✅ 显示 **AI 最终回复**（白色聊天气泡）
  - 内容类似："你好！我是你的任务管理助手..."
- ✅ **不显示** Action 或 Observation 卡片
- ✅ 控制台显示：`✅ ReactAgent 初始化成功`

**常见问题**：
- ❌ 如果没有显示 Thought 卡片 → 检查是否成功切换到 Agent 模式
- ❌ 如果显示错误 → 检查 API Key 是否配置正确

---

### ✅ 场景 2: 查询任务（调用单个工具）

**目的**：验证 Agent 能调用 `get_tasks` 工具查询任务。

**步骤**：
1. 确保处于 **Agent 模式**
2. 输入：`我今天有哪些任务？`
3. 发送消息

**预期结果**：
- ✅ 显示 **Agent Thought** 卡片（紫色）
  - 内容类似："用户想查看今天的任务，我需要调用 get_tasks 工具..."
- ✅ 显示 **Agent Action** 卡片（蓝色）
  - 工具名称：`get_tasks` 🔍
  - 工具描述："查询用户任务"
  - 点击"查看参数"能展开参数 JSON
  - 参数包含：`userId`, `dateRange`, `includeCompleted`
- ✅ 显示 **Agent Observation** 卡片（绿色或红色）
  - 如果成功：绿色，显示 "✅ 工具执行成功"
  - 点击"查看结果"能展开结果 JSON
  - 结果包含任务列表
- ✅ 显示 **AI 最终回复**
  - 内容类似："你今天有 X 个任务：..."

**完整流程**：
```
用户消息 → Thought → Action → Observation → AI 回复
```

**常见问题**：
- ❌ 如果 Observation 显示错误（红色）→ 检查数据库连接
- ❌ 如果任务列表为空 → 确保今天有创建任务

---

### ✅ 场景 3: 分析任务（调用多个工具）

**目的**：验证 Agent 能连续调用多个工具。

**步骤**：
1. 确保处于 **Agent 模式**
2. 输入：`帮我分析今天的任务`
3. 发送消息

**预期结果**：
- ✅ 显示第 1 轮推理：
  - Thought: "需要先获取今天的任务..."
  - Action: `get_tasks`
  - Observation: 任务列表
- ✅ 显示第 2 轮推理：
  - Thought: "现在调用 analyze_tasks 分析这些任务..."
  - Action: `analyze_tasks`
  - Observation: 分析结果
- ✅ 显示 **AI 最终回复**
  - 包含任务分析结果、优先级建议等

**完整流程**：
```
用户消息 → Thought₁ → Action₁ → Observation₁ → Thought₂ → Action₂ → Observation₂ → AI 回复
```

**验证点**：
- ✅ 多个 Thought 卡片按顺序显示
- ✅ 每个 Action 对应一个 Observation
- ✅ 最多显示 5 轮推理（Agent 配置的最大迭代次数）

---

### ✅ 场景 4: 交互式工具（澄清任务）

**目的**：验证 Agent 能正确暂停并等待用户输入。

**步骤**：
1. 确保处于 **Agent 模式**
2. 先创建一个模糊的任务：
   - 点击左上角 ➕ 按钮
   - 输入任务标题：`完成项目报告`
   - 优先级：高
   - 保存
3. 输入：`帮我澄清任务：完成项目报告`
4. 发送消息

**预期结果**：

**第 1 阶段（Agent 暂停）**：
- ✅ 显示 **Agent Thought** 卡片
  - 内容类似："用户想澄清任务，我需要调用 clarify_task..."
- ✅ 显示 **Agent Action** 卡片
  - 工具名称：`clarify_task`
- ✅ 显示 **Agent Need Input** 卡片（黄色）
  - 标题："⏸️ Agent 需要您的输入"
  - 显示澄清问题（多个问题）
  - 显示文本输入框
  - 显示"✅ 提交并继续"按钮
- ✅ 输入框被禁用，显示："🤖 Agent 正在思考，请稍候..."

**第 2 阶段（用户输入后）**：
1. 在 Need Input 卡片中输入答案：
   ```
   这个报告是关于 Q3 季度的销售数据分析，
   预计需要 2 天完成，
   主要包括数据收集、分析和撰写报告。
   ```
2. 点击"✅ 提交并继续"

**第 3 阶段（Agent 恢复）**：
- ✅ Need Input 卡片变为禁用状态（显示"✅ 已提交"）
- ✅ 显示用户输入的消息
- ✅ 显示 **Agent Observation** 卡片
  - 工具名称：`clarify_task`
  - 显示澄清后的任务信息
- ✅ 显示 **AI 最终回复**
  - 包含澄清后的任务详情

**完整流程**：
```
用户消息 → Thought → Action (clarify_task) → [Agent 暂停] → 
Need Input 卡片 → 用户输入 → [Agent 恢复] → Observation → AI 回复
```

---

### ✅ 场景 5: 交互式工具（任务拆解）

**目的**：验证另一个交互式工具的流程。

**步骤**：
1. 确保处于 **Agent 模式**
2. 输入：`帮我把"学习 React"这个任务拆成小步骤`
3. 发送消息

**预期结果**：
- ✅ 类似场景 4，但调用的是 `decompose_task` 工具
- ✅ Need Input 卡片询问任务相关问题
- ✅ 用户输入后，Agent 返回拆解后的子任务列表

---

### ✅ 场景 6: 错误处理

**目的**：验证 Agent 能正确处理工具执行失败。

**步骤**：
1. **临时断开数据库连接**（或修改代码模拟错误）：
   - 打开 `task-manager/src/lib/agent/tools/GetTasksTool.ts`
   - 在 `execute` 方法开头添加：
     ```typescript
     throw new Error('模拟数据库连接超时');
     ```
2. 确保处于 **Agent 模式**
3. 输入：`我今天有哪些任务？`
4. 发送消息

**预期结果**：
- ✅ 显示 **Agent Thought** 卡片
- ✅ 显示 **Agent Action** 卡片（`get_tasks`）
- ✅ 显示 **Agent Observation** 卡片（**红色**）
  - 显示 "❌ 模拟数据库连接超时"
  - 错误图标
- ✅ Agent 继续推理或返回错误提示

**记得恢复代码**：删除刚才添加的 `throw` 语句！

---

### ✅ 场景 7: 最大迭代次数限制

**目的**：验证 Agent 达到最大迭代次数后会停止。

**步骤**：
1. 打开 `task-manager/src/lib/agent/AgentConfig.ts`
2. 临时修改 `maxIterations` 为 `2`：
   ```typescript
   export const DEFAULT_AGENT_CONFIG: AgentConfig = {
     maxIterations: 2,  // 改为 2（原来是 5）
     // ...
   }
   ```
3. 刷新页面
4. 确保处于 **Agent 模式**
5. 输入一个需要多步推理的问题：`帮我分析并拆解任务`

**预期结果**：
- ✅ 显示第 1 轮推理（Thought → Action → Observation）
- ✅ 显示第 2 轮推理（Thought → Action → Observation）
- ✅ 达到 2 次迭代后停止
- ✅ 显示提示："已达到最大推理次数..."

**记得恢复**：将 `maxIterations` 改回 `5`！

---

### ✅ 场景 8: 模式切换

**目的**：验证 Agent 模式和普通模式可以无缝切换。

**步骤**：

**Part 1: 普通模式发送消息**
1. 确保处于 **普通模式**（开关关闭）
2. 输入：`你好`
3. 发送消息

**预期结果**：
- ✅ 显示普通的 AI 回复（白色聊天气泡）
- ✅ **不显示** Thought、Action、Observation 卡片

**Part 2: 切换到 Agent 模式**
1. 点击顶部的模式切换开关
2. 确认切换到 **Agent 模式**（显示紫色渐变提示条）
3. 输入：`我今天有哪些任务？`
4. 发送消息

**预期结果**：
- ✅ 显示 Agent 推理过程（Thought → Action → Observation）
- ✅ 之前的普通模式消息仍然保留
- ✅ Agent 模式立即生效

**Part 3: 切回普通模式**
1. 再次点击模式切换开关
2. 确认切换到 **普通模式**
3. 输入：`谢谢`
4. 发送消息

**预期结果**：
- ✅ 显示普通的 AI 回复
- ✅ 之前的 Agent 消息仍然保留
- ✅ 模式切换不影响对话历史

**验证点**：
- ✅ 模式切换即时生效
- ✅ 对话历史不丢失
- ✅ 控制台显示模式切换日志：`🔧 AI 助手模式切换为: Agent 模式`

---

## 📋 测试检查清单

在完成所有场景测试后，检查以下项：

### 核心功能
- [ ] Agent 初始化成功（控制台无错误）
- [ ] Agent 模式切换开关工作正常
- [ ] 消息发送时正确区分 Agent/普通模式
- [ ] Agent 返回结果正确解析
- [ ] Thought、Action、Observation 卡片正确渲染
- [ ] Need Input 卡片能正确暂停和恢复
- [ ] 加载指示器显示和消失正常
- [ ] 错误消息正确显示

### 用户体验
- [ ] Agent 模式提示条显示正确（紫色渐变）
- [ ] 示例问题卡片显示正确
- [ ] 输入框在 Agent 运行时禁用（紫色背景）
- [ ] Placeholder 正确更新（"Agent 正在思考..."）
- [ ] 自动滚动到最新消息
- [ ] 折叠/展开功能（参数、结果）正常工作
- [ ] 时间戳正确显示（无 Hydration 错误）

### 移动端适配
- [ ] 缩小浏览器窗口，所有卡片正常显示
- [ ] 文本不溢出
- [ ] 按钮可点击
- [ ] 滚动流畅

### 与现有功能兼容性
- [ ] 与工作流模式无冲突
- [ ] 任务识别模式仍正常工作
- [ ] 图片上传功能正常
- [ ] 任务创建/编辑功能正常

### 性能
- [ ] Agent 首次调用时间 < 10 秒
- [ ] 连续对话响应流畅
- [ ] 页面不卡顿
- [ ] 控制台无内存泄漏警告

---

## 🐛 常见问题和解决方案

### 问题 1: Agent 不响应
**症状**：点击发送后没有任何反应

**解决方案**：
1. 检查控制台是否有错误
2. 确认 API Key 已配置
3. 检查网络请求是否成功
4. 刷新页面重试

---

### 问题 2: Thought/Action/Observation 卡片不显示
**症状**：只显示普通的 AI 回复

**解决方案**：
1. 确认已切换到 **Agent 模式**（顶部开关）
2. 检查 `localStorage.getItem('ai_assistant_mode')` 是否为 `'agent'`
3. 刷新页面

---

### 问题 3: 输入框无法输入
**症状**：输入框显示禁用状态

**解决方案**：
1. 检查是否有工作流模式正在运行（如任务拆解、澄清）
2. 等待 Agent 完成推理
3. 刷新页面

---

### 问题 4: Need Input 卡片提交后无响应
**症状**：点击"提交并继续"后 Agent 不恢复

**解决方案**：
1. 检查控制台是否有错误
2. 确认 `onAgentInputSubmit` 函数被正确调用
3. 检查 `agentResumeContext` 是否正确保存

---

## ✅ 测试完成标准

当以下所有条件满足时，Step 6 测试通过：

1. ✅ 所有 8 个测试场景都通过
2. ✅ 测试检查清单中所有项都打勾
3. ✅ 控制台无严重错误
4. ✅ 移动端显示正常
5. ✅ 与现有功能无冲突

---

## 📝 测试报告模板

测试完成后，请记录以下信息：

```markdown
## Phase 4 测试报告

**测试日期**：2025-11-07
**测试人员**：[Your Name]
**浏览器**：Chrome 120 / Firefox 121 / Safari 17

### 场景测试结果
- [ ] 场景 1: 简单对话 - ✅ 通过 / ❌ 失败
- [ ] 场景 2: 查询任务 - ✅ 通过 / ❌ 失败
- [ ] 场景 3: 分析任务 - ✅ 通过 / ❌ 失败
- [ ] 场景 4: 澄清任务 - ✅ 通过 / ❌ 失败
- [ ] 场景 5: 任务拆解 - ✅ 通过 / ❌ 失败
- [ ] 场景 6: 错误处理 - ✅ 通过 / ❌ 失败
- [ ] 场景 7: 最大迭代 - ✅ 通过 / ❌ 失败
- [ ] 场景 8: 模式切换 - ✅ 通过 / ❌ 失败

### 发现的问题
1. [问题描述]
2. [问题描述]

### 建议改进
1. [改进建议]
2. [改进建议]

### 总体评价
[ ] 🟢 优秀 - 所有功能正常，无明显问题
[ ] 🟡 良好 - 大部分功能正常，有小问题
[ ] 🔴 需改进 - 有严重问题需要修复
```

---

**测试愉快！** 🚀

有任何问题请查看"常见问题和解决方案"部分，或在控制台查看详细错误信息。





