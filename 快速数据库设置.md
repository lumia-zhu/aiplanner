# 🗄️ Supabase 数据库快速设置

## 步骤 1️⃣：访问 Supabase SQL Editor

1. 登录 https://supabase.com
2. 选择您的项目（或创建新项目）
3. 在左侧菜单中点击 **SQL Editor**

---

## 步骤 2️⃣：执行数据库脚本

### 方法一：完整导入（推荐）

在 SQL Editor 中，点击 **New Query**，然后复制粘贴项目中的 `database/schema.sql` 文件内容并执行。

这个脚本会创建：
- ✅ `users` 表 - 用户信息
- ✅ `tasks` 表 - 任务数据
- ✅ 自动更新时间的触发器
- ✅ 查询性能索引
- ✅ 行级安全策略（RLS）

### 方法二：分步导入

如果您想了解每个表的作用，可以按需执行 `database/` 目录中的单个脚本：

```sql
-- 基础表
database/create-daily-tasks-table.sql      -- 任务表（必需）
database/create-user-profiles-table.sql    -- 用户配置表
database/create-notes-table.sql            -- 笔记表
database/create-chat-messages-table.sql    -- AI聊天消息表
database/create-sticky-notes-table.sql     -- 便签表
database/create-task-matrix-table.sql      -- 任务矩阵表

-- 其他功能表（可选）
database/add-task-tags.sql                 -- 任务标签
database/add-subtask-support.sql           -- 子任务支持
database/create-reflection-tables.sql      -- 反思记录表
```

---

## 步骤 3️⃣：验证安装

执行以下 SQL 查询，检查表是否创建成功：

```sql
-- 查看所有表
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public';

-- 检查 RLS 状态
SELECT tablename, rowsecurity 
FROM pg_tables 
WHERE schemaname = 'public';
```

---

## 步骤 4️⃣：获取 API 密钥

1. 在 Supabase 项目中，点击左侧 **Settings** → **API**
2. 复制以下信息到 `.env.local` 文件：
   - **Project URL** → `NEXT_PUBLIC_SUPABASE_URL`
   - **anon public key** → `NEXT_PUBLIC_SUPABASE_ANON_KEY`

---

## ⚠️ 重要提示

### 关于 RLS（行级安全策略）

数据库脚本启用了 RLS 安全策略，这意味着：
- ✅ 用户只能访问自己的数据
- ✅ 提高了数据安全性
- ⚠️ 需要正确的用户认证才能操作数据

### 如果遇到权限问题

如果在开发过程中遇到权限错误，可以临时禁用 RLS（仅用于调试）：

```sql
-- 临时禁用 RLS（仅开发环境）
ALTER TABLE users DISABLE ROW LEVEL SECURITY;
ALTER TABLE tasks DISABLE ROW LEVEL SECURITY;

-- 生产环境请保持启用
ALTER TABLE users ENABLE ROW LEVEL SECURITY;
ALTER TABLE tasks ENABLE ROW LEVEL SECURITY;
```

---

## 📝 测试数据（可选）

如果您想添加一些测试数据：

```sql
-- 执行 database/sample-data.sql
-- 或 database/user-profiles-sample-data.sql
```

---

## ✅ 完成！

数据库设置完成后，您就可以：
1. 配置好 `.env.local` 文件
2. 运行 `npm run dev`
3. 访问 http://localhost:3000
4. 开始使用任务管理器！

---

**需要帮助？** 查看 `database/README.md` 了解更多数据库相关信息。






