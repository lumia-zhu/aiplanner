# 🧪 澄清问题显示修复 - 测试指南

## ✅ 已完成的修复

### P0 修复（必须）
1. ✅ **添加降级问题库** - 确保即使 LLM 失败也有 3 个问题显示
2. ✅ **添加数组边界检查** - 防止访问不存在的问题导致 undefined

### P1 修复（重要）
3. ✅ **修改 Prompt** - 明确要求 LLM 必须生成恰好 3 个问题
4. ✅ **问题数量验证** - 自动补充问题到 3 个

### P2 修复（优化）
5. ✅ **UI 空值处理** - 当问题为空时显示友好提示

---

## 🔧 修改的文件

### 1. `src/lib/reflectionFlow.ts`
**添加内容：**
- 降级问题库常量 `FALLBACK_QUESTIONS`（每个轮次 3 个备用问题）
- 问题数量验证和补充逻辑
- LLM 调用失败时的降级处理
- JSON 解析失败时使用降级问题
- 强化 Prompt 明确要求 3 个问题

**关键代码：**
```typescript
// 降级问题库
const FALLBACK_QUESTIONS: Record<ReflectionRoundType, ReflectionQuestion[]> = {
  clarity: [
    { text: '任务具体是要修改哪方面的内容？', ... },
    { text: '修改的重点是什么？', ... },
    { text: '预期的完成标准是什么？', ... }
  ],
  // ... time 和 priority
}

// 问题数量验证
if (questions.length < 3) {
  console.warn('补充问题到3个')
  for (let i = questions.length; i < 3; i++) {
    questions.push(FALLBACK_QUESTIONS[round][i])
  }
}
```

### 2. `src/app/notes-dashboard/page.tsx`
**修改内容：**
- 添加数组边界检查
- 问题不存在时记录错误日志
- 使用安全的默认值

**关键代码：**
```typescript
const nextQuestion = totalQuestions[nextIndex] || '问题加载失败，请跳过或返回重试'

if (!totalQuestions[nextIndex]) {
  console.error(`问题数组越界：访问 [${nextIndex}]，数组长度 ${totalQuestions.length}`)
}
```

### 3. `src/components/ChatSidebar.tsx`
**修改内容：**
- QuestionAnswerCard 组件添加空值保护
- 显示友好的错误提示

**关键代码：**
```typescript
{question || '⚠️ 问题加载失败，您可以跳过此问题或返回重试'}
```

---

## 📋 测试步骤

### 测试 1：正常场景（LLM 成功生成 3 个问题）

**步骤：**
1. 重启开发服务器：`npm run dev`
2. 登录应用
3. 在笔记编辑器中创建任务："CHI Revision"
4. 保存笔记
5. 点击右下角 AI 助手图标
6. 点击 "📝 每日反思" 按钮
7. 在 Clarity 轮选择 "CHI Revision" 任务

**预期结果：**
- ✅ 显示 3 个完整的问题
- ✅ 问题 1/3、2/3、3/3 都能正常显示
- ✅ 每个问题都有具体内容
- ✅ 可以输入答案或跳过
- ✅ 点击"下一个问题"平滑过渡
- ✅ 最后一个问题显示"完成 ✓"按钮

---

### 测试 2：降级场景（模拟 LLM 返回不完整问题）

**步骤：**
1. 临时修改网络延迟或 API 限流
2. 按照测试 1 的步骤操作
3. 观察问题显示

**预期结果：**
- ✅ 即使 LLM 只返回 1 个问题，也会自动补充到 3 个
- ✅ 补充的问题来自降级问题库
- ✅ 所有 3 个问题都能正常显示和交互

**降级问题示例：**
```
问题 1/3: （可能是 LLM 生成的）
问题 2/3: "修改的重点是什么（如内容、格式、逻辑、数据等）？"（降级问题）
问题 3/3: "预期的完成标准是什么？怎样算修改完成？"（降级问题）
```

---

### 测试 3：极端场景（LLM 完全失败）

**步骤：**
1. 临时注释掉 API Key 或使用错误的 Key
2. 按照测试 1 的步骤操作

**预期结果：**
- ✅ 显示 3 个降级问题（全部来自问题库）
- ✅ 控制台显示警告：`⚠️ 使用降级问题库`
- ✅ 用户体验不受影响，可以正常回答问题

---

### 测试 4：不同轮次的问题

**步骤：**
1. 完成 Clarity 轮后，进入 Time 轮
2. 选择一个有时间估算的任务
3. 观察问题显示

**预期结果：**
- ✅ Time 轮也显示 3 个问题
- ✅ 问题与时间规划相关
- ✅ 如果 LLM 失败，显示 Time 轮的降级问题

**Time 轮降级问题：**
```
1. "回想一下上次做类似任务的时候，实际花费的时间是不是比预想的要长？"
2. "做这个任务之前，是不是需要等别人的回复或准备什么？"
3. "如果中途遇到卡壳或意外情况，有预留缓冲时间吗？"
```

---

## 🔍 调试检查点

### 控制台日志

**正常情况下应该看到：**
```
✅ 生成了 3 个问题
```

**问题补充时应该看到：**
```
⚠️ 只生成了 1 个问题，使用降级问题补充到3个
✅ 补充后共 3 个问题
```

**LLM 失败时应该看到：**
```
❌ LLM 调用失败: [错误信息]
⚠️ 使用降级问题库
```

**数组越界时应该看到：**
```
⚠️ 问题数组越界：尝试访问 totalQuestions[2]，但数组长度为 1
```

---

## 🐛 常见问题排查

### 问题 1：仍然显示空白问题

**检查：**
1. 控制台是否有 `问题数组越界` 错误？
2. `totalQuestions` 数组内容是什么？
3. LLM 是否成功返回？

**解决：**
- 查看控制台日志
- 确认 API Key 配置正确
- 检查网络连接

### 问题 2：问题不是 3 个

**检查：**
1. 控制台是否显示补充日志？
2. `FALLBACK_QUESTIONS` 是否正确定义？

**解决：**
- 确认 `reflectionFlow.ts` 的修改已保存
- 重启开发服务器

### 问题 3：降级问题没有生效

**检查：**
1. 是否重启了开发服务器？
2. 浏览器缓存是否清除？

**解决：**
```bash
# 硬重载浏览器（Ctrl+Shift+R）
# 或清除浏览器缓存
# 重启开发服务器
npm run dev
```

---

## ✨ 修复效果对比

### 修复前：
```
问题 1/3: ✅ 显示正常
问题 2/3: ❌ 空白（undefined）
问题 3/3: ❌ 空白（undefined）
```

### 修复后：
```
问题 1/3: ✅ LLM 生成的问题
问题 2/3: ✅ 降级问题（如果需要）
问题 3/3: ✅ 降级问题（如果需要）
```

---

## 📊 测试检查清单

- [ ] Clarity 轮显示 3 个问题
- [ ] Time 轮显示 3 个问题  
- [ ] Priority 轮显示 3 个问题
- [ ] 问题内容不为空
- [ ] 可以输入答案
- [ ] 可以跳过问题
- [ ] "下一个问题"按钮正常工作
- [ ] 最后一个问题显示"完成 ✓"
- [ ] LLM 失败时使用降级问题
- [ ] 控制台没有错误（除了预期的警告）

---

## 🎯 测试通过标准

**所有以下条件都满足：**
1. ✅ 3 个问题都能正常显示
2. ✅ 每个问题都有文本内容（不是 undefined 或空）
3. ✅ 交互流程顺畅（输入、跳过、下一个）
4. ✅ 降级机制正常工作
5. ✅ 用户体验良好（没有卡顿或错误提示）

---

**测试完成后，请在此记录结果：**

测试日期：____________
测试人员：____________
测试结果：[ ] 通过  [ ] 失败
问题记录：____________

