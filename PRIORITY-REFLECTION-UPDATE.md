# 📊 优先级反思流程优化（V2）

## 🎯 优化目标

将优先级排序从**"考试式提问"**转变为**"场景化引导"**，帮助用户：
1. **不直接问抽象概念**（如"哪个重要"），而是**通过具体场景**自然引导
2. **不让用户分类**（如"是否紧急"），而是**让用户描述后果**，AI 来推断
3. **不评判用户**，而是**观察行为**（如"在拖延哪个"）来识别真实优先级

## 💡 核心理念：不考试，只引导

---

## 🔄 改动内容

### 1. **优先级问题生成策略** 

#### 改动前：
- 主要关注任务对比和后果分析
- 直接询问"哪个任务先做"
- 缺少维度选择引导

#### 改动后：
- **三步走策略**：
  - 第一步：维度选择（紧急性 vs 重要性 vs 影响力等）
  - 第二步：任务对比（基于用户选择的维度）
  - 第三步：象限定位（判断任务属于哪个象限）

### 2. **问题设计转变**

#### ❌ 旧方式（直接提问，像考试）：
```
- "你现在更关心任务的紧急性还是重要性？"
- "「原型开发」是不是紧急又重要的事？"
- "哪个任务对你的目标影响更大？"
```

#### ✅ 新方式（场景化引导，像对话）：

**🔹 后果反推法**（引导识别紧迫性）
```
- "如果「原型开发」推迟到下周，会发生什么？"
- "「原型开发」和「QE准备」，哪个拖得越久越麻烦？"
- "有没有人在等你的「原型开发」结果？大概什么时候需要？"
```

**🔹 场景假设法**（引导对比和权衡）
```
- "这周如果只能完成一件事，你最希望是哪个？为什么？"
- "如果明天有临时紧急事务，这几个任务里哪个可以先放一放？"
- "假设你现在精力充沛，会从哪个任务开始？"
```

**🔹 行为观察法**（识别真实优先级）
```
- "「原型开发」和「QE准备」，哪个你一直在拖延？"
- "哪个任务你想到就焦虑，但又一直没开始？"
- "哪个任务做完后，你会觉得最轻松或最有成就感？"
```

**🔹 依赖探索法**（识别执行顺序）
```
- "做完「原型开发」之后，会让其他什么事情变简单？"
- "这几个任务里，哪个是其他任务的前置条件？"
```

### 3. **问题数量调整**

- **改动前**：1-3 个问题
- **改动后**：2-4 个问题（确保包含维度选择）

### 4. **降级问题更新（V2）**

#### FALLBACK_QUESTIONS
```typescript
priority: [
  {
    id: 'priority-fallback-1',
    text: '这周如果只能完成一件事，你最希望是哪个？为什么？',
    hint: '通过假设场景识别真正重要的事'  // ✅ 场景化
  },
  {
    id: 'priority-fallback-2',
    text: '这几个任务里，哪个拖得越久越麻烦？',
    hint: '通过后果反推紧迫性'  // ✅ 后果导向
  },
  {
    id: 'priority-fallback-3',
    text: '有没有人在等你某个任务的结果？大概什么时候需要？',
    hint: '识别外部依赖和真实deadline'  // ✅ 具体情境
  },
  {
    id: 'priority-fallback-4',
    text: '哪个任务你一直在拖延？通常拖延的原因是什么？',
    hint: '通过行为识别重要但困难的任务'  // ✅ 行为观察
  }
]
```

### 5. **Prompt 优化**

#### buildRoundPrompt (priority)
- 新增**第一步：维度选择**指导
- 新增**第三步：象限定位**指导
- 强调问题必须包含具体任务名称或维度概念
- 输出格式调整为 3-4 个问题

#### generatePriorityQuestions
- 系统 prompt 完全重写，强调三步走策略
- 用户 prompt 明确要求包含维度选择问题
- 降级问题全部更新，确保包含维度选择

---

## 🎯 预期效果

### V1 优化（太直接）：
```
AI: 你现在更关心任务的紧急性还是重要性？
用户: 呃...我不知道...（困惑）

AI: 「原型开发」是不是紧急又重要的事？
用户: 应该算吧？（不确定）
```
**问题**：像在考试，用户需要已经懂"紧急性/重要性"框架才能回答。

### V2 优化（场景引导）：
```
AI: 如果「原型开发」推迟到下周，会发生什么？
用户: 嗯...会影响到后面的 QE 准备时间...（自然描述后果）

AI: 这周如果只能完成一件事，你最希望是哪个？
用户: 应该是原型开发吧，因为后面的任务都要等它...（自然表达重要性）

AI: 「原型开发」和「QE准备」，哪个你一直在拖延？
用户: QE准备...因为不知道从哪开始...（识别障碍）
```

**AI 内部推断**（不告诉用户）：
- ✅ 原型开发是"重要不紧急"（有依赖，但没立即 deadline）
- ✅ QE准备虽然重要，但用户在拖延，可能需要先澄清
- ✅ 建议顺序：先做原型开发，再处理 QE 准备

**优势**：
- ✅ **降低认知负担**：用户只需描述事实，不需理解框架
- ✅ **更真实的回答**：基于具体情境，而非抽象判断
- ✅ **识别隐藏信息**：通过"拖延"等行为识别真实障碍
- ✅ **ADHD 友好**：具体、可视化、非评判性

---

## 🧪 测试场景

### 场景 1：多个任务，无明确 deadline
```
任务列表：
- 原型开发
- QE准备
- 看看TA批改作业的要求
```

**预期问题**：
1. 你现在更关心任务的紧急性（是否有deadline）还是重要性（对目标的影响）？
2. 「原型开发」和「QE准备」相比，哪个对你的目标影响更大？
3. 这几个任务中，哪个是真正的"重要不紧急"任务？
4. 如果今天只能做一个，不做哪个任务明天会更麻烦？

### 场景 2：多个任务，有 deadline
```
任务列表：
- 原型开发 (deadline: 明天)
- QE准备
```

**预期问题**：
1. 你更关心哪个维度？紧急性还是重要性？
2. 「原型开发」有明确的 deadline，但「QE准备」对长期影响更大，你会优先做哪个？
3. 「原型开发」是不是属于"紧急且重要"的象限？

---

## 📝 相关文件

- `aiplanner/src/lib/reflectionFlow.ts`
  - `buildRoundPrompt` (priority 部分)
  - `generatePriorityQuestions`
  - `FALLBACK_QUESTIONS`

---

## 🎓 教育意义

### V1 vs V2 的本质区别：

| 维度 | V1（直接提问） | V2（场景引导） |
|-----|---------------|---------------|
| **认知要求** | 需要懂"紧急性/重要性"框架 | 只需描述具体事实 |
| **回答难度** | 需要抽象思考和分类 | 基于直觉和经验回答 |
| **心理压力** | 感觉像考试，怕答错 | 像聊天，无压力 |
| **ADHD 友好** | ❌ 抽象概念难理解 | ✅ 具体场景易想象 |
| **教育效果** | 教框架（但用户可能不懂） | 潜移默化形成直觉 |

### 这次优化的核心价值：

1. **降低认知门槛**：不需要用户先学会优先级理论
2. **真实而非正确**：获取用户的真实感受，而非"应该"的答案
3. **识别隐藏障碍**：通过"拖延"等行为发现真正的问题
4. **渐进式教育**：多次使用后，用户自然理解什么是"重要且紧急"

**最重要的是**：用户不会觉得被"教育"，而是觉得被"理解"。🎯

